/**
 * Instant CLI / dashboard: zelfde schema als de Ionic-app (`medtrackerInstantSchema`).
 * Push vanaf projectroot: zie package.json script `instant:push`.
 */
import { medtrackerInstantSchema } from './src/app/core/medtracker-instant-schema';

export default medtrackerInstantSchema;
